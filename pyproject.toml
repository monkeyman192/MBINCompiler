[project]
name = "mbincompiler"
version = "1.0.0"
description = "Unit test and extraction tools for MBINCompiler"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest",
    "requests",
    "jinja2",
    "pymem"
]
